#!/usr/bin/env python3
"""
STOA Agent CLI launcher (dev-mode, no install).

Mirrors the installed `stoa` console-script for users who clone the repo
and run the CLI without `pip install -e .`. After install, prefer the
installed entry point.
"""

if __name__ == "__main__":
    from stoa_cli.main import main
    main()
